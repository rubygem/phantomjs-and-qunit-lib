var MongoRepository = require('mongorepository').Repository;

var TransitQuoteViewStore = function(){
	
	function get(quoteId, callback){
		var query = {"_id": quoteId};
		var quoteRepository = new MongoRepository(process.env.VIEW_STORE_CONNECTION_STRING,'quotes-transit');
		quoteRepository.find(query, function(results){
				callback(results[0]);
			});
	}

	return {
		get:get
	};
};

module.exports = TransitQuoteViewStore;